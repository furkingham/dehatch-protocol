// Single source of truth for the anchor integration.
// Moving to a production anchor = change these values (SEP-1 discovers the rest).
export const ANCHOR_HOME_DOMAIN = "tr-mock-anchor.fly.dev";
export const ANCHOR_URL = `https://${ANCHOR_HOME_DOMAIN}`;
export const NETWORK_PASSPHRASE = "Test SDF Network ; September 2015";
export const HORIZON_URL = "https://horizon-testnet.stellar.org";
export const ASSET_CODE = "USDC";
